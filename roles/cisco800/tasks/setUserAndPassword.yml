- name: Add new User and Uniformise Passwords
  ios_config:
      lines:
        - username {{ newUser }} previlege {{ newUserPrivilege | default(15) }} secret {{ numberPasswordStrong }}
        - enable secret {{ numberPasswordStrong }}
      save_when: changed